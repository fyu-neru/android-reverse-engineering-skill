FILE:plugins/android-reverse-engineering/skills/android-reverse-engineering/scripts/lib/tools.sh
FIND:    line="${line%$'\r'}"
REPLACE:    :
EXPECT:tools.sh: tool_field's raw output bytes (checked via file redirection, not a pipe) contain no embedded \r from a CRLF-saved row
