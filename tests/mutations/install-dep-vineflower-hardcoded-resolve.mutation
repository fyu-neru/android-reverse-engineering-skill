FILE:plugins/android-reverse-engineering/skills/android-reverse-engineering/scripts/install-dep.sh
FIND:  if resolved=$(tool_resolve vineflower); then
REPLACE:  if command -v vineflower &>/dev/null || command -v fernflower &>/dev/null; then
EXPECT:resolves an already-installed jar via the VINEFLOWER_JAR env override with nothing on PATH
